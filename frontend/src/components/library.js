import e from '../element.js';
import c from '../component.js';

export default class Library extends c {
  constructor(props) {
    super(props);
  }

  render() {
    return e("section", {id: "library"},
			
    )
  }
}